server:
  port: 8080

#need to check what good practice and stuff for this is
spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/backenddb
    username: sathvik
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
     hibernate:
       dialect: org.hibernate.dialect.PostgreSQLDialect
       format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
  sql:
    init:
      platform: postgres

security:
  jwt:
    token:
      secret-key: 7c9324123ec96dfc5ccf7a200d1dd0bd0dfdaba08f776f20edcfdc060fc34045